defaults: &defaults
  # slaves:
  #   - host: slave1.local
  #     port: 27018
  #   - host: slave2.local
  #     port: 27019

development:
  <<: *defaults
  host: localhost
  database: rp-mon-dev

test:
  <<: *defaults
  host: localhost
  database: rp-mon-test

production:
  <<: *defaults
  uri: <%= ENV['MONGODB_URI'] %>
